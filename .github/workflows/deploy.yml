name: Deploy to Production

on:
  push:
    branches:
      - main

# Deploy otomatis menggunakan GitHub Webhook ke public/webhook.php
# Setup: GitHub repo -> Settings -> Webhooks -> https://tubagus.my.id/webhook.php
